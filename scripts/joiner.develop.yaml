---
packages:
  - packages/*
  - fixtures/*

package:
  manager: yarn
  publisher: npm
  ignore:
    - packages/elementql-spec
    - node_modules

runFrom: ../

development:
  watchPackages: all
  serverPort: 55000
  watchDirectories: ['build', 'distribution', 'dist']
  externalPackages: []
